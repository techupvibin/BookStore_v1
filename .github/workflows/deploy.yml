name: Deploy to EC2

on:
  workflow_run:
#    workflows: ["build-images"]   # Trigger when build-images workflow completes
#    types:
#      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ---------------------------
      # Set up SSH key for EC2
      # ---------------------------
      - name: Copy SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key

      - name: Test SSH Connection (Optional)
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/ec2_key ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} "echo 'SSH connection successful!'"

      # ---------------------------
      # Deploy on EC2
      # ---------------------------
      - name: Deploy Docker containers
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/ec2_key ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'

          # Login to ECR
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} \
            | docker login --username AWS --password-stdin ${{ secrets.ECR_URI }}

          # Pull latest images
          docker pull ${{ secrets.ECR_URI }}/bookstore-backend:latest
          docker pull ${{ secrets.ECR_URI }}/bookstore-frontend:latest

          # Stop and remove old containers (ignore errors)
          docker stop backend-container || true
          docker stop frontend-container || true
          docker rm backend-container || true
          docker rm frontend-container || true

          # Run backend
          docker run -d --name backend-container -p 8080:8080 \
            ${{ secrets.ECR_URI }}/bookstore-backend:latest

          # Run frontend
          docker run -d --name frontend-container -p 80:80 \
            ${{ secrets.ECR_URI }}/bookstore-frontend:latest

          EOF
